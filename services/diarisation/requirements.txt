fastapi==0.115.0
uvicorn==0.30.6
# pyannote.audio 4.x uses the modern huggingface_hub token= API (compatible with
# huggingface_hub >=1.0) and requires torch>=2.8, so the previous torch<2.6 pin
# is intentionally gone — 4.x is built for the newer torch and handles the
# Lightning-checkpoint weights_only load that <2.6 guarded against. torch,
# torchaudio and torchcodec are pulled transitively; torchcodec needs FFmpeg
# shared libraries, which the Dockerfile installs (ffmpeg package).
pyannote.audio==4.0.7
scipy
soundfile
numpy
