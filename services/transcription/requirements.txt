fastapi==0.115.0
uvicorn==0.30.6
faster-whisper==1.2.1
# ctranslate2 4.5+ links against system CUDA 12 + cuDNN 9 (provided by the base
# image) instead of bundling cuDNN, and its wheel no longer carries the
# executable-stack ELF flag. That lets this image drop three earlier
# workarounds: the nvidia-cudnn-cu12 wheel, the LD_LIBRARY_PATH juggling, and
# fix_execstack.py. faster-whisper 1.2.1 permits ctranslate2<5 and no longer
# imports `requests` directly (uses huggingface_hub), so that pin is gone too.
ctranslate2==4.8.1
