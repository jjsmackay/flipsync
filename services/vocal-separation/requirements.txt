fastapi==0.115.0
uvicorn==0.30.6
demucs==4.0.1
# torch must stay <2.6: demucs 4.0.1 loads its checkpoints via torch.load,
# which defaults to weights_only=True from 2.6 and breaks pickled model objects.
torch<2.6
torchaudio<2.6
# torchaudio 2.5 registers no I/O backend on its own (no bundled ffmpeg
# integration, apt's ffmpeg CLI doesn't count) — torchaudio.load/save raise
# "Couldn't find appropriate backend" without this.
soundfile
numpy

# RoFormer backend (see docs/superpowers/plans/notes/2026-07-13-roformer-dep-solve.md).
# Pins below are the versions the dep-solve spike confirmed coexist with
# demucs 4.0.1 under torch<2.6. onnx is pinned explicitly — otherwise the
# resolver floats onto an onnx-weekly dev build via audio-separator.
audio-separator==0.44.3
onnxruntime-gpu==1.27.0
onnx==1.17.0
