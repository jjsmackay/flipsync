import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>
      <h1>FlipSync</h1>
      <p>Frontend coming in Wave 5.</p>
    </div>
  </React.StrictMode>,
)
