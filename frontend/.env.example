# Optional — comma-separated list of extra hostnames the Vite dev/preview
# server will accept (e.g. behind a reverse proxy or custom local domain).
# Unset by default, meaning only localhost is allowed.
# VITE_ALLOWED_HOSTS=your.dev.hostname
